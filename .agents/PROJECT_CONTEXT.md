# 项目跟进记录

稳定操作约束见根目录 `AGENTS.md`；本文件只记录会随项目推进变化的事实。

## 当前项目目标

- 当前目标：维护独立 Windows BLE DUT 模拟器上位机，通过最多 6 路 USB 串口配置 ESP32 模拟节点，并保留单文件 EXE 交付流程。依据：`README.md`、`app.py`。
- 当前范围：`products.json` 含从已配置上位机数据库导出的 22 个 BLE 产品，覆盖 ES4、ES5、S5、E5、H3、E7D、CE1/C6DB、E6D；包含实际名称、完整 Model 和默认字段值。
- 验收分层：协议/存储自动化测试；Windows GUI/打包检查；真实 COM、ESP32、BLE、六节点和治具硬件验证。

## 架构和模块现状

- `app.py` - active；Tkinter UI 和业务编排，管理产品编辑、6 个节点、命令待确认状态及串口事件队列。
- `protocol.py` - active；产品族 schema、Payload 打包、BLE 名称编解码和 `CONFIG` 命令格式。
- `serial_node.py` - active；pyserial 适配、后台读线程、写锁、序号和 `@SIM` 解析。
- `storage.py` / `products.json` - active；SQLite CRUD、动态协议族定义、兼容迁移与仅首次导入的种子数据。
- `tests/` - active；覆盖无需硬件的协议、模板、串口默认参数与存储回归。
- `build/`、`*.spec`、`__pycache__/` - generated；可删除重建。`dist/` - generated/deliverable；未确认交付需求前不清理。
- `build_app.ps1` - active 打包入口；`start.bat` 已按用户要求删除。

## 关键决策记录

- 2026-08-19 - 按用户要求删除 `start.bat` 及其文档引用；源码仍可通过 `python app.py` 启动，EXE 继续由 `build_app.ps1` 生成。
- 2026-08-19 - Windows 端配置串口统一为 115200 baud；UART0 与 GPIO1/3 是 ESP32 固件侧约定，Windows 端只选择 COM 口。
- 2026-08-18 - 清理生成缓存和 IDE 私有元数据，但保留 `dist/` 现有 EXE：当前目录没有 Git，且 README 将 EXE 作为推荐运行方式。
- 2026-08-18 - 项目指南以当前源码为准；ESP32 固件行为只记录为外部边界。
- 2026-08-19 - PID 继续作为主广播包中的 MiBeacon 产品身份；Model 作为 Scan Response 中的 BLE Complete Local Name，UTF-8 最多 29 字节，通过 `CONFIG` 第 10 个字段下发。
- 2026-08-19 - `%LOCALAPPDATA%` 现有数据库是用户配置真源；`products.json` 仅作为新数据库默认种子，不覆盖已存在模板。未被产品引用的自定义协议族 `cb` 不进入默认库。

## 已完成变更

- 2026-08-19 - 协议族由固定代码定义扩展为 SQLite 持久化配置；支持新增、复制、编辑和删除未引用的自定义协议族，字段可配置名称、顺序、默认值、`u8/s8/u16/s16/u32/s32` 类型及数组数量。
- 2026-08-19 - 新品模板可选择动态协议族；编辑共享协议族时同步迁移引用产品字段，兼容值保留、不兼容值回退默认值；字段编辑区支持滚动。
- 2026-08-19 - 模板参数区移除挤占协议族输入宽度的行内管理按钮；在协议字段标题区提供“管理协议族”和“编辑字段名称/结构”入口，字段名称可直接进入结构编辑器修改。
- 2026-08-19 - 修复协议族编辑器新增字段后名称列被压缩为 1 px：表头与数据行统一列宽，字段名称和默认值列共同弹性伸缩。
- 2026-08-19 - 移除原始 Payload HEX、手动校验按钮和校验结果条；保存与应用配置仍自动校验。协议族管理和字段结构编辑按钮移至底部操作栏。
- 2026-08-19 - 模板底部操作栏改为三个等宽按钮列，按钮占满中间面板可用宽度，使用两个固定 8 px 间隔列，左右无额外留白。
- 2026-08-19 - 产品模板列表的产品名称列改为居中对齐，与居中的表头、PID 和 Model 列保持一致。

- 2026-08-18 - 新增 `AGENTS.md` 和本文件，记录入口、职责、线程/事件流、协议/存储边界、验证与维护流程。
- 2026-08-18 - 将 `.idea/` 加入 `.gitignore`；清理 `.idea/`、`__pycache__/`、`build/` 和时间戳 `.spec`，保留源码、测试、脚本及 `dist/`。
- 2026-08-18 - 重新规整三栏 UI：移除重复串口统计和节点冗余状态文字，在右侧底部保留唯一统计；选择框改为紧凑方形；左侧显示所选 UART 的产品、PID、BLE 名称和广播状态。
- 2026-08-18 - `CONFIG` 命令追加 Model 的 UTF-8 HEX；连接后主动查询 `STATUS`，并从 `model_hex` 恢复 ESP32 当前配置。
- 2026-08-18 - 窗口、任务栏和 PyInstaller EXE 统一使用黑底白字、保持方形比例的 `linp` 图标资源。

- 2026-08-19 - 日志收起高度改为使用标题栏实际请求高度，避免高 DPI 下固定 48 px 裁切标题与按钮。
- 2026-08-19 - Windows 顶层窗口首次映射后通过 `WM_SETICON` 设置 32/16 px `linp.ico`，退出时释放原生图标句柄。
- 2026-08-19 - 默认模板库按当前配置数据库的 22 个产品重新导出；保留完整 BLE 名称及 E6D 单键、CE1-T2、C6DB 等已调整字段值。
- 2026-08-19 - 搜索改为监听文本值变化并在回车/失焦时清理首尾空白；产品列表改为独立三列，产品和 Model 左对齐、PID 居中、Model 弹性占用剩余宽度；默认窗口调整为 `2200×1600`。
- 2026-08-19 - 搜索输入在每次变化时立即清理首尾空白并显式左对齐，保留实时筛选；产品表按可用宽度均分产品/PID/Model 三列，产品/PID/Model 分别左/中/左对齐，默认左栏目标宽度为 680 px，使右侧节点区获得更多宽度。
- 2026-08-19 - 运行日志从底部全宽区域移入 ESP32 节点右栏下方，固定展开并保留清空、自动滚动和 1000 行上限；普通日志为白底深色文本，发送/成功/警告/错误分别采用蓝/绿/棕/红语义色。
- 2026-08-20 - 产品模板表采用产品靠左、PID 位于产品与 Model 文字的视觉中点、Model 在第三列居中的三段布局；默认宽度完整显示最长 Model。模板参数保持名称独占一行、Model/PID 同行、协议族/异常模式同行且输入边界对齐，窄窗自动切换紧凑排列；三个模板操作按钮固定在面板底部。串口汇总独立放在 UART6 与运行日志之间，日志标题仅保留标题和清空按钮；按参考截图将默认窗口调整为 `2100×1400`、最小窗口调整为 `1260×900`。
- 2026-08-20 - 附件中的 22 个产品模板作为名称、Model、PID、时序和字段值真源；种子文件使用与实际字段结构对应的内部协议族 ID（E5_1..4、H3_1..3、T2DSB、C6），这些 ID 不进入发往 ESP32 的 `CONFIG` 字段，生成的 Payload 与附件一致。

- 2026-08-20 - 界面“协议”采用产品级名称：E5/H3/E6D/E7D 不区分按键数，CE1/CE1-T2/CE1-PRO 显示 CE1，C6DB 显示 C6；数据库内部结构 ID 继续决定字段结构和 Payload，不作为用户协议名显示。
- 2026-08-20 - 主模板区“协议”恢复为可选下拉框，只显示公开协议名；数据库新增独立 protocol_name 并兼容迁移旧库，内部 family 继续决定字段结构和 Payload。切换 E5/H3 且无法继承当前结构时，先选择具体字段结构。

## 当前风险和债务

- 环境风险：系统 `python` 命中 Microsoft Store 占位符；需使用可用 Python 3、Tkinter 和 pyserial 或明确的 Python 路径。
- 协议风险：支持 29 字节 Model 的上位机需要配套 0.2.1 固件；旧 0.2.0 固件的名称缓冲区仍仅支持 18 字节。`DISCONNECT`、ACK/ERROR 和真实广播名称仍需硬件联调确认。
- 持久化风险：`meta.seeded` 使 `products.json` 只在首次建库时导入；修改种子不会迁移已有用户数据库。协议族表会在现有数据库首次启动新版时自动创建，编辑共享协议族会影响所有引用产品，界面会先提示。
- 并发风险：串口线程通过队列切回 Tk 主线程；修改关闭、重连或事件处理时需检查重复 `LOCAL_DISCONNECTED` 和状态清理。

## 维护入口

- 产品族：`protocol.py:FAMILY_SCHEMAS` -> `products.json` -> `tests/test_protocol.py`。
- 配置/控制命令：`protocol.py:build_config_command`、`app.py:_apply_to_nodes` / `_send_selected` -> `_handle_node_event`。
- 串口：`serial_node.py:SerialNode` -> `app.py:_toggle_node` / `_queue_node_event` / `_handle_node_event`。
- 存储：`storage.py:ProductStore` -> `tests/test_storage.py`；字段变化先设计兼容迁移。
- UI 状态：`app.py:SimulatorApp`；用测试或串口日志确认状态来源，不用延时掩盖问题。

## 验证历史

- 2026-08-18 - `python -m unittest discover -s tests -v` - `blocked`；系统 `python` 是 Microsoft Store 占位符，测试未启动，不是用例失败。
- 2026-08-18 - `python -m py_compile app.py protocol.py serial_node.py storage.py` - `blocked`；同上。
- 2026-08-18 - 工作区自带 Python 执行单元测试 - `verified`；11 个用例全部通过。
- 2026-08-18 - 工作区自带 Python 执行 `py_compile` - `verified`；`app.py`、`protocol.py`、`serial_node.py`、`storage.py` 全部通过。
- 2026-08-18 - 源码静态追踪 - `verified`；确认入口、存储初始化、串口线程到 Tk 队列、命令及 ACK/ERROR 状态流。
- 2026-08-18 - Model/BLE 名称协议回归 - `verified`；13 个单元测试全部通过，包含 UTF-8 往返、18 字节边界及新 `CONFIG` 格式。
- 2026-08-18 - 修改后 `py_compile` - `verified`；`app.py`、`protocol.py`、`serial_node.py`、`storage.py` 全部通过。
- 2026-08-18 - 隔离 GUI 实窗验证 - `verified`；窗口状态为 `normal`，客户区 `1974×1498` 并居中，三栏宽度 `608/676/646`，没有截断或重叠；小屏幕会按屏幕边界收敛。
- 2026-08-18 - PyInstaller 打包 - `verified`；生成 `dist/BLE产品产测模拟器-V1-202608181703.exe`，窗口不再调用 `state("zoomed")`；未连接硬件。
- 2026-08-18 - 硬件验证 - `documented`；尚未连接真实串口、刷写 ESP32、验证 BLE 广播名称、六节点或治具。

- 2026-08-19 - 日志收起回归脚本 - `verified`；收起容器与标题栏请求高度均为 62 px，按钮底部为 53 px，标题和按钮完整显示。
- 2026-08-19 - Windows 标题栏图标 - `verified`；源码实窗截图显示黑底白字 `linp`，最终 EXE 的 `WM_GETICON` 返回有效 16/32 px 图标句柄。
- 2026-08-19 - 修改后回归 - `verified`；13 个单元测试全部通过，`app.py`、`protocol.py`、`serial_node.py`、`storage.py` 语法检查通过。
- 2026-08-19 - 动态协议族回归 - `verified`；18 个单元测试全部通过，覆盖自定义协议族 CRUD、32 位字段、64 字节上限、字段兼容迁移和内置协议族保护；主窗口及两个协议族对话框通过隐藏式实窗启动检查。
- 2026-08-19 - 协议字段 UI 复核 - `verified`；1600×1000 实窗交互检查确认两个管理按钮不重叠、协议族输入宽度充足，并确认字段名称输入框可编辑。
- 2026-08-19 - 字段名称列回归 - `verified`；使用真实 `cb` 协议族点击“添加字段”复现修复前名称输入框仅 1 px，修复后两行均为 167 px，并显示 `version` / `field_2`。
- 2026-08-19 - Payload 校验区精简回归 - `verified`；18 个单元测试和 `py_compile` 全部通过；实窗冒烟确认“原始 Payload”“校验 Payload”及结果条已移除，三个模板操作按钮位于同一底部操作栏，保存路径仍会拒绝超出 `u8` 范围的 `256`。
- 2026-08-19 - 模板底部按钮栏回归 - `verified`；实窗像素检查确认操作栏宽 595 px，三个按钮均为 193 px，间隔均为 8 px，左右余量均为 0；18 个单元测试和 `py_compile` 全部通过。
- 2026-08-19 - 产品名称列对齐回归 - `verified`；运行时 `#0` 列锚点为 `center`，实窗截图确认表头与产品名称同轴居中；18 个单元测试和 `py_compile` 全部通过。
- 2026-08-19 - 最终打包 - `verified`；生成 `dist/BLE产品产测模拟器-V1-202608190938.exe`，随后清理旧 EXE、`build/`、`*.spec` 和 Python 缓存。
- 2026-08-19 - 长 BLE 名称回归 - `verified`；19 个单元测试和四个核心模块语法检查通过，`linp.sensor_occupy.es4b` 的 23 字节 UTF-8 Model 可编码，30 字节名称被拒绝；配套 0.2.1 模拟固件已构建成功。未刷机或进行真实 Scan Response、BLE 和治具联调。
- 2026-08-19 - 配置模板导出回归 - `verified`；新 `products.json` 与当前 `products.db` 的 22 个产品逐字段一致，20 个单元测试和四个核心模块语法检查通过。
- 2026-08-19 - 产品列表与搜索实窗回归 - `verified`；真实 DPI 环境窗口为 `2200×1600`，Model 列 451 px、最长名称测量 242 px，三列对齐/伸缩符合设计，带首尾空白的 `ES4` 搜索归一化后仍唯一匹配。截图已生成，`view_image` 因本机 ACL 沙箱故障未能读取。
- 2026-08-19 - 本次验证未连接真实 COM、ESP32、BLE、六节点或治具；只覆盖默认模板、Python 协议/存储与 Windows 源码实窗。
- 2026-08-19 - 最终打包与首次建库冒烟 - `verified`；生成 `dist/BLE产品产测模拟器-V1-202608191650.exe`（SHA-256 `AF94E74AD20BCB16ACE3DADDF2728B9A374FFBB14883C9038DE5ED22F4218006`）。临时 `LOCALAPPDATA` 首次运行创建 22 个产品和 13 个内置协议族，未包含 `cb`，ES4/E6D 单键配置正确；隐藏启动未暴露主窗口句柄，确认建库后终止测试进程，打包窗口视觉未单独验证。
- 2026-08-19 - 搜索、模板表和日志实窗回归 - `verified`；隔离窗口在当前可用屏幕收敛到 `1829×1143`，搜索 `  39181  ` 立即成为 `39181` 并筛到 1 项；产品树宽 776 px、三列各 258 px，最长 Model 140 px；右栏日志顶端 y=453，节点列表底端 y=443。白底普通/蓝绿棕红语义日志文本对比度均不低于 4.5:1，收起与展开正常。
- 2026-08-19 - 最小窗口布局回归 - `verified`；`1260×760` 下产品树三列各 102 px，右栏日志高度 158 px，未与节点或底部串口统计重叠。较窄窗口无法完整展示最长 Model，默认窗口及当前可用屏幕可以完整展示。
- 2026-08-19 - 界面修复 EXE 打包与首次启动 - `verified`；生成 `dist/BLE产品产测模拟器-V1-202608191744.exe`（SHA-256 `5E79B415DC6AD7F07987CC852F5D4E197F1FC6077D725B7A7A8FFF94EBCA8FE8`）。隔离 `LOCALAPPDATA` 首次启动创建 22 个模板和 13 个内置协议族，未带入自定义 `cb`；未连接真实 COM、ESP32、BLE、六节点或治具。
- 2026-08-19 - 日志固定展开与左栏均衡回归 - `verified`；20 个单元测试全部通过。`1829×1143` 实窗三栏宽度为 `680/571/534`，产品表三列各 216 px，最长 Model 140 px；`1260×760` 下日志正文仍有 105 px 高度，且与节点列表保持 10 px 间隔。
- 2026-08-19 - 固定日志版本 EXE 打包与首次启动 - `verified`；生成并复制 `BLE产品产测模拟器-V1-202608191807.exe`，SHA-256 为 `9D2E31C9CA3E7D63EF2005A3E3FB351A4BACB5A3C19261A8783414DF72787DA7`。隔离 `LOCALAPPDATA` 首次启动创建 22 个模板和 13 个内置协议族；未连接真实 COM、ESP32、BLE、六节点或治具。
- 2026-08-20 - 产品表、模板编辑器与日志标题布局回归 - verified：Windows Per-Monitor V2 高 DPI（Tk scaling 2.3324）下，默认 650 px 产品表三列为 284/80/284 px，PID 整表中心偏差 0 px，最长 Model 需求 262 px并完整显示；模板参数为原始三行位置，左侧输入统一从 x=184 开始，右侧输入统一从 x=591 开始且宽 177 px，Model 输入宽 291 px。三个模板操作按钮固定在底部，仅保留 15 px 面板内边距。1260×760 下表单切换紧凑排列，Model 宽 312 px；8 字段画布缩为 83 px并滚动，与操作栏保持 10 px。逐字搜索“E5 四键”、搜索列宽稳定、空库、往返缩放、12 串口汇总换行均通过；20 个单元测试、四个核心模块 py_compile 与 git diff --check 均通过。未连接真实 COM、ESP32、BLE 或治具。

- 2026-08-20 - 最终三栏布局回归 - verified：默认窗口目标为 `2200×1100`，当前屏幕收敛为 `1829×1100`；Model 表头和单元格均居中。UART6 到串口汇总条间距 8 px，汇总条到运行日志间距 10 px，日志正文可见高度 453 px；ES5 8 个字段完整显示，操作栏仍固定在中栏底部。隔离数据库截图、四个核心模块 py_compile、20 个单元测试和 git diff --check 均通过。`1260×760` 最小高度下右栏日志正文会被 6 个节点和汇总条压缩为 0 px，此边界未在本次确认范围内调整。未连接真实 COM、ESP32、BLE、六节点或治具。

- 2026-08-20 - 最终布局版 EXE 打包与文件校验 - verified：生成并复制 C:\Users\kyrie\Desktop\上位机\BLE产品产测模拟器-V1-202608201122.exe，SHA-256 为 58FC4C08705A4FEA959DB88B91EEC464EC5EACCDE37EA48E380A53DDCB05050C；复制前后哈希一致，已删除本轮被替代的 11:15 临时交付件。最终 EXE 未启动，以避免单文件子进程清理误伤用户正在运行的同名程序；源码实窗、隔离数据库、20 个单元测试和核心模块语法检查已通过。未连接真实 COM、ESP32、BLE、六节点或治具。

- 2026-08-20 - Model 居中与串口汇总位置版 EXE 打包 - verified：生成并复制 `C:\Users\kyrie\Desktop\上位机\BLE产品产测模拟器-V1-202608201150.exe`，SHA-256 为 `AB2799085DC0724BB17F50648487C6A664A25FAADBCD1AD8C9C99998996E00E4`，大小 12,656,180 字节，复制前后哈希一致。被替代的 11:22 版本仍有两个运行进程，Windows 拒绝删除，因此保留且未强制结束进程；新 EXE 未启动。未连接真实 COM、ESP32、BLE、六节点或治具。

- 2026-08-20 - 参考截图窗口尺寸回归 - verified：Per-Monitor V2 高 DPI 下默认窗口为 `2100×1400` 并居中，三栏宽度 `680/761/615`，ES5 字段完整显示，日志正文 618 px；最小 `1260×900` 下 ES5 字段区自动滚动，日志正文仍有 118 px，所有按钮均在窗口内。两档均保持 UART6、8 px、串口汇总、10 px、运行日志的顺序；20 个单元测试、四个核心模块 py_compile 与 git diff --check 均通过。低于 `1260×900` 的物理可用屏幕可能被最小尺寸超出。未连接真实 COM、ESP32、BLE、六节点或治具。

- 2026-08-20 - 参考截图尺寸版 EXE 打包 - verified：生成并复制 `C:\Users\kyrie\Desktop\上位机\BLE产品产测模拟器-V1-202608201341.exe`，SHA-256 为 `80F0053B4DB068807D6D801FB41934CC512E1D576D7E90C78705B0F8CC31CAD0`，大小 12,656,707 字节，复制前后哈希一致；已删除被替代的 11:50 和 11:22 旧交付件（删除前均校验路径、哈希与运行状态）。新 EXE 未启动；未连接真实 COM、ESP32、BLE、六节点或治具。

- 2026-08-20 - 附件协议与 PID 视觉中点回归 - `verified`；附件与 `products.json` 的 22 个模板除必要内部协议族 ID 外完全一致，Payload 逐项一致；全新数据库创建 22 个模板和 13 个内置协议族。20 个单元测试及核心模块 `py_compile` 全部通过。当前可用屏幕下产品表为 `213/70/365` px，PID 两侧视觉间距 `217.3/217.5` px，误差 `0.2` px，Model 需求 160 px 并完整显示。未连接真实 COM、ESP32、BLE、六节点或治具。

- 2026-08-20 - 附件协议与 PID 视觉中点版 EXE 打包 - `verified`；生成并复制 `C:\Users\kyrie\Desktop\上位机\BLE产品产测模拟器-V1-202608201453.exe`，SHA-256 为 `64FA0781C43A0C179382852D876F74960906E55547DB42C71977CE0A5810295D`，大小 12,656,537 字节，复制前后哈希一致；保留旧版本且未启动新 EXE。未连接真实 COM、ESP32、BLE、六节点或治具。

- 2026-08-20 - 公开协议名与内部字段结构分离回归 - verified；22 个单元测试和四个核心模块 py_compile 全部通过。隔离 Tk 窗口逐项加载 22 个模板，公开协议名无 _1..4 或 T2DSB，C6DB 显示 C6、CE1 系列显示 CE1；保存前收集的内部 family 与原模板一致，22 个 Payload 逐字节一致。未连接真实 COM、ESP32、BLE、六节点或治具。

- 2026-08-20 - 公开协议名修正版 EXE 打包 - verified；生成并复制 C:\Users\kyrie\Desktop\上位机\BLE产品产测模拟器-V1-202608201607.exe，SHA-256 为 1BDDB11B860231C8F439C2593132AB2014D5D54E09AA682F0300A7DB044C265E，大小 12,656,730 字节，复制前后哈希一致；保留旧版本且未启动新 EXE。未连接真实 COM、ESP32、BLE、六节点或治具。

- 2026-08-20 - 可选协议与持久化迁移回归 - verified；主协议控件为只读下拉选择，公开协议名与内部字段结构分别保存。24 个单元测试和四个核心模块 py_compile 全部通过；隔离 Tk 窗口确认 CE1/C6 独立保存、E5 原结构保留及 E5/H3 跨协议结构选择。CONFIG/Payload 格式未变；未连接真实 COM、ESP32、BLE、六节点或治具。
- 2026-08-20 - 可选协议版 EXE 打包 - verified；生成并复制 C:\Users\kyrie\Desktop\上位机\BLE产品产测模拟器-V1-202608201726.exe，SHA-256 为 D24A2C2274A119DACA014A2BD2CDC00AF441AF3691648E4B0E535F9C89E395A1，大小 12,661,842 字节，复制前后哈希一致并保留旧版本。新 EXE 未启动；未连接真实 COM、ESP32、BLE、六节点或治具。
## 下一步建议

- 下一次代码任务先用可用 Python 运行现有 24 个单元测试，再按需求追踪对应模块。
- 涉及固件协议或 BLE 异常时，需要匹配版本的 ESP32 固件源码或完整 `@SIM` 串口日志。

